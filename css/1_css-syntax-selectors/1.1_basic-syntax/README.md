# 🎨 Basic CSS Syntax

## 🎯 Purpose:

This section introduces the **basic syntax of CSS**, which is the foundational skill needed to style HTML documents. It covers how to select elements, assign properties, and define values for styling, along with how to use comments and properly link CSS to HTML files.

---

## 📑 Core Concepts

| **Concept**        | **Description**          |
| ------------------ | ------------------------ |
| `<link>` | External resource linking tag      |
| `CSS Comments`  | Non-executed notes          |
| `Selector`        | Targets HTML elements     |
| `Property`       | Defines the style feature  |
| `Value`         | Sets how the feature looks  |

---

## 📌 Summary

- **`<link rel='stylsheet>`**: connects an external CSS file to the HTML document.
- **CSS comments**: start with `/*` and end with `*/`. They are ignored by the browser.
- Declaration block is enclosed in `{}` and contains `property-value` pairs.
- Each declaration ends with a semicolon `;`

⏭️ **Next:** Lets explore Selectors in CSS